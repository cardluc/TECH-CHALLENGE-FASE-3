pandas>=2.2,<3
pyarrow>=14.0,<26
numpy>=1.24,<3
# >=1.6: learning_curve e validation_curve só aceitam `params` a partir dessa
# versão, e é por ali que o peso amostral chega ao passo do modelo
scikit-learn>=1.6,<2
shap>=0.44,<1
matplotlib>=3.8,<4
tabulate>=0.9,<1

# enriquecimento externo (IBGE via BigQuery)
pandas-gbq>=0.22,<1

# testes
pytest>=8.0,<9
